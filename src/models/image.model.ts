export interface Image {
  url: string;
}

export interface CloudinaryImage {
  id: string;
  filename: string;
  aspect_ratio: number;
  asset_id: string;
  bytes: number;
  folder: string;
  format: string;
  height: number;
  pixels: number;
  public_id: string;
  resource_type: string;
  secure_url: string;
  status: string;
  width: number;
}
